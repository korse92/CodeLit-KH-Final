<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="studyBoard">

	<select id="studyBoardList" resultType="studyBoard">
		select 
		    rownum,
		    n.*
		from(
		    select 
		        * 
		    from 
		        study_board
		    order by 
		        STD_BRD_NO
		    asc) n
		order by
		    rownum
		    desc
	</select>
	
	<select id="getListCount" resultType="_int" >
		SELECT
			COUNT(*)
		FROM
			study_board
	</select>
		<select id="selectOneStudyBoard" resultType="notice">
		SELECT
			*
		FROM
			study_board
		WHERE
			std_brd_no = #{stdBrdNo}
	</select>
	
	<insert id="insertBoard">
		INSERT INTO
		study_board(
			std_brd_no,
			ref_member_id,
			ref_lecture_no,
			std_brd_title,
			std_brd_content,
			std_brd_date,
			std_brd_count
			)
		values(
			SEQ_STD_BRD_NO.NEXTVAL,
			#{refMemberId},
			5,
			#{stdBrdTitle},
			#{stdBrdContent},
			default,
			0
		)
		<selectKey resultType="_int" keyProperty="stdBrdNo" order="AFTER">
		select
			SEQ_STD_BRD_NO.currval
		from
			dual
		</selectKey>
	</insert>
	<delete id="delete">
		DELETE
		FROM
			study_board
		where
			notice_no = #{noticeNo}
	</delete>
	<update id="update">
		update 
		    study_board
		set 
		    notice_title = #{noticeTitle},
		    notice_content = #{noticeContent},
		    notice_date = default
		where
		    notice_no = #{noticeNo}
	</update>
	<update id="updateCnt">
		update
			study_board
		set
			notice_count = notice_count+1
		where
			notice_no = #{noticeNo}
	</update>
	<select id="selectAttachment" resultType="com.kh.codelit.attachment.model.vo.Attachment">
		select 
		    a.*,
		    c.contents_attach_path
		from 
		    attachment a  
		JOIN 
		    contents_group c 
		ON 
		    a.ref_contents_group_no = c.contents_group_code
		where
		    a.ref_contents_code = #{noticeNo}
		and 
			a.ref_contents_group_no = 'N'
	</select>
	<delete id="deleteAttach">
		delete 
		from 
			attachment 
		where 
			ref_contents_code = #{noticeNo} 
		and 
			ref_contents_group_no = 'N'
	</delete>


</mapper>